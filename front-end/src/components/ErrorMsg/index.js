import styled from 'styled-components';

const ErrorMsg = styled.span`
  color: var(--dark-gray);
  font-weight: 500;
  font-size: 0.8em;
  text-align: center;
  width: 100%;
`;

export default ErrorMsg;
